server:
  port: 8083

spring:
  application:
    name: foodRecord
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql_database:3306/foodRecord
    username: sergio
    password: hermanos16?

  jpa:
    hibernate:
      ddl-auto: create-drop
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect

